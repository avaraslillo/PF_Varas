import { ConcatpipePipe } from './concatpipe.pipe';

describe('ConcatpipePipe', () => {
  it('create an instance', () => {
    const pipe = new ConcatpipePipe();
    expect(pipe).toBeTruthy();
  });
});
